SublimeRoom is a plugin for subliem text 2 that allows writeroom like cursor centering.  

when coupled with distraction free mode, this is rather nice for writing prose. 

When installed, you can toggle the cursor centering with a keystroke per view.